import styled from "@emotion/styled";
import { setSize } from "@sun-river/components";

export const ImageBox = styled.picture`
  position: relative;

  ${setSize("20rem", "20rem")}

  overflow: hidden;
`;
